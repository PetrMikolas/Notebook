@if (!Visible) return;

<div class="spinner-container">
    <span id="spinner-text">Načítání...</span>
    <div class="spinner"></div>
</div>

@code {
    [Parameter] public bool Visible { get; set; } = true;
}